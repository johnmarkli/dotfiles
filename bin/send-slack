#!/bin/bash

if [ $# -eq 0 ]
then
  # Prompt for destination
  read -e -r -p 'Send to:
> ' destination
else
  destination=$1
fi
slack-cli -s $destination -l 5

read -e -r -p 'Message to send:
> ' message

slack-cli -d $destination "$message"
